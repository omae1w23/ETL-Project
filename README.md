# ETL-Project
This project implements an ETL pipeline using SQL Server Integration Services (SSIS) for a telecom company. It processes CSV files generated every 5 minutes, applies data validation and transformation rules, and loads the results into a data warehouse.
